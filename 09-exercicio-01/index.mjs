import { createFile, showFile, updateFile, deleteFile } from "./functions.mjs";

createFile("Conteúdo inicial do arquivo | Criando com o módulo fs do Node.js");
showFile();
updateFile("Novo conteúdo de testes.")
deleteFile();

